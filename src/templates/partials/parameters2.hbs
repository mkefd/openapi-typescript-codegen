{{#if parameters}}
	{
	{{#each parameters}}
		{{{name}}}{{>isRequired}}: {{>type}},
	{{/each}}
	}
	{{#if hasDefault}}
		= {
		{{#each parameters}}
			{{#if default}}
				{{{name}}}: {{{default}}},
			{{/if}}
		{{/each}}
		}
	{{/if}}
{{else}}
	{}
{{/if}}

